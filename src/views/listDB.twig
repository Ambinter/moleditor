{% if dbs %}
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">My Databases <b class="caret"></b></a>
        <ul class="dropdown-menu">
			{% for db in dbs %}
				<li><a href="{{ path('display', {'dbname':db}) }}">{{db}}</a></li>
			{% endfor %}
        </ul>
      </li>
{% endif %}
